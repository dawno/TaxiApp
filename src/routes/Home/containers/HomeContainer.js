import {connect} from "react-redux";
import Home from "../components/Home";
import {}from "../modules/home";
const mapsStateToProps = (state) => ({

});


const mapActionCreator ={};
export default connect(mapsStateToProps,mapActionCreator)(Home);